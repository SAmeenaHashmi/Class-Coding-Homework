int main()
// code that calculates the factorial of a number

{
    int input;
    int fact = 1;
    printf("Enter a number:");
    scanf("%d", &input);
    for (int i = input; i > 0; --i) {
        fact = fact * i;
    }
    printf("%d", fact);
    return 0;
}
